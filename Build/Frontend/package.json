{
  "name": "backend-ip-login-frontend",
  "description": "Build tooling for the backend_ip_login TYPO3 extension frontend assets.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "eslint": "^10.4.0",
    "globals": "^15.14.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  }
}
